[project]
name = "ssac-splitsecond"
version = "0.1.0"
description = "SSAC SplitSecond swim video analyzer"
requires-python = ">=3.10"
dependencies = ["opencv-python", "numpy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
